# Installation Instructions
# =======================
# We recommend using uv for faster dependency management.
#
# Setup:
#   uv venv
#   source .venv/bin/activate  # On macOS/Linux
#   uv pip install -r requirements.txt
#   uv pip install -e ../../packages/evaris-py
#
# Or install directly:
#   uv pip install sentence-transformers openai google-generativeai

# Core dependencies (should be installed with evaris)
pydantic>=2.0.0

# LLM provider dependencies
openai>=1.0.0
google-generativeai>=0.3.0

# Optional: For semantic similarity metric
sentence-transformers>=2.2.0

# Note: Evaris framework must be installed separately from repository root
# uv pip install -e /path/to/evaris/packages/evaris-py
